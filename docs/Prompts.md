Create an AI chat page with tabs according to this document (HI business model innovation).
What's important is that, for each prompt corresponding to a workflow, that prompt goes through the 5 steps without losing context. There should be 5 steps inside a conversation rather than some conversations in each step.


Can you make it so when we access the next step it hides the conversation for the previous step (can still be access by clicking the previous tab) and it sends a default prompt corresponding to the step or the chatbot directly sends a message ?


Please add :
- A possibility for the user to send a link to their company in step 1
- A business model canva filled by the chatbot during step 1 that should be validated, and it should be updated during the chat
- A next step after step 5 which creates a roadmap (with expected times and AI/human indications) and updates the business model canva
Fix table display during the chat, right now it's plain text but it should ba a table


Could you add a dropdown to chose a LLM ? I can give you API keys later if needed ; if that's the case, make a placeholder for now